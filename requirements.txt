# ═══════════════════════════════════════════════════════════
# Nexira / Ultimate AI System v8.0 — Python Dependencies
# Created by Xeeker & Claude — February 2026
#
# Install: pip install -r requirements.txt
# Or just run: ./install.sh (handles everything)
# ═══════════════════════════════════════════════════════════

# ── Core Web Framework (required) ─────────────────────────
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SocketIO==5.3.6
simple-websocket>=0.9.0

# ── AI / LLM (required) ──────────────────────────────────
ollama>=0.1.6

# ── Networking (required) ─────────────────────────────────
requests>=2.31.0

# ── Encryption (required for password/journal encryption) ─
cryptography>=41.0.0

# ── Date/Time Utilities (required) ────────────────────────
python-dateutil>=2.8.2
pytz>=2023.3

# ── System Monitoring (required) ──────────────────────────
psutil>=5.9.6

# ── Utilities (required) ─────────────────────────────────
python-dotenv>=1.0.0
croniter>=2.0.1

# ── Document Processing (optional — gracefully disabled) ──
PyPDF2>=3.0.1
python-docx>=1.1.0
Pillow>=10.1.0
pytesseract>=0.3.10

# ── Email IMAP Monitoring (optional) ─────────────────────
imapclient>=2.3.1

# ── GPU / Image Generation (optional — NVIDIA only) ──────
# Uncomment these if you have an NVIDIA GPU with CUDA:
# torch
# diffusers
# transformers
